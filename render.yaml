services:
  - type: web
    name: resume-screening-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    runtime: python
    region: oregon
    branch: main
    repo: https://github.com/bashirubash/resume_screening_ai
